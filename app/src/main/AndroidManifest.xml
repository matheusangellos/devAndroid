<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalho1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trabalho1">
        <activity android:name=".DetalhePostActivity" android:parentActivityName=".DetalhePostActivity" />
        <activity android:name=".DetalhePhotoActivity" android:parentActivityName=".DetalhePhotoActivity" />
        <activity android:name=".DetalheCommentActivity" android:parentActivityName=".DetalheCommentActivity" />
        <activity android:name=".PostActivity" android:parentActivityName=".PostActivity" />
        <activity android:name=".PhotoActivity" android:parentActivityName=".PhotoActivity" />
        <activity android:name=".CommentActivity" android:parentActivityName=".CommentActivity" />
        <activity
            android:name=".TodoActivity"
            android:parentActivityName=".TodoActivity" />
        <activity android:name=".DetalheTodoActivity" android:parentActivityName=".DetalheTodoActivity" />
        <activity
            android:name=".HomeActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>